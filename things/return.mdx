---
$type: Verb
$id: https://schema.org.ai/return
$context: https://schema.org.ai
name: return
description: The act of returning to the origin that which was previously received (concrete objects) or taken (ownership).
status: public
license: MIT
source: schema.org
schemaVersion: '14.0'
category: Action
conjugation:
  base: return
  thirdPerson: returns
  past: returned
  objectNoun: returning
  opposite: keep
  inverse: returnedBy
---

# return

The act of returning to the origin that which was previously received (concrete objects) or taken (ownership).

## Schema.org Reference

[return](https://schema.org/ReturnAction) in Schema.org vocabulary.

## Usage

```typescript
import { $ } from 'sdk.do'

// Use this type in your code
const result = await db.query({
  subject: $.return,
})
```
