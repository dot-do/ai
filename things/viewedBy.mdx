---
$type: Verb
$id: https://schema.org.ai/viewedBy
$context: https://schema.org.ai
name: viewedBy
description: Inverse of view (passive/reverse relationship)
status: public
license: MIT
source: schema.org
schemaVersion: '14.0'
category: Action
---

# viewedBy

Inverse of view (passive/reverse relationship)

## Schema.org Reference

[viewedBy](https://schema.org/ViewAction) in Schema.org vocabulary.

## Usage

```typescript
import { $ } from 'sdk.do'

// Use this type in your code
const result = await db.query({
  subject: $.viewedBy,
})
```
