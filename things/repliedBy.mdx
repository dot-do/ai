---
$type: Verb
$id: https://schema.org.ai/repliedBy
$context: https://schema.org.ai
name: repliedBy
description: Inverse of reply (passive/reverse relationship)
status: public
license: MIT
source: schema.org
schemaVersion: '14.0'
category: Action
---

# repliedBy

Inverse of reply (passive/reverse relationship)

## Schema.org Reference

[repliedBy](https://schema.org/ReplyAction) in Schema.org vocabulary.

## Usage

```typescript
import { $ } from 'sdk.do'

// Use this type in your code
const result = await db.query({
  subject: $.repliedBy,
})
```
