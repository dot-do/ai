---
$type: Verb
$id: https://schema.org.ai/returnedBy
$context: https://schema.org.ai
name: returnedBy
description: Inverse of return (passive/reverse relationship)
status: public
license: MIT
source: schema.org
schemaVersion: '14.0'
category: Action
---

# returnedBy

Inverse of return (passive/reverse relationship)

## Schema.org Reference

[returnedBy](https://schema.org/ReturnAction) in Schema.org vocabulary.

## Usage

```typescript
import { $ } from 'sdk.do'

// Use this type in your code
const result = await db.query({
  subject: $.returnedBy,
})
```
