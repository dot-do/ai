---
$type: Technology
$id: https://tech.org.ai/React
$context: https://schema.org
name: React
commodityCode: '2021-10-03'
category: Framework
hotTechnology: true
vendor: Meta (Facebook)
description: JavaScript library for building user interfaces
datePublished: '2013-05-29'
version: '18.3'
license: CC-BY-4.0
url: https://react.dev
---

# React

React is a free and open-source JavaScript library for building user interfaces, particularly single-page applications. Created by Jordan Walke at Facebook and released in 2013, React has become one of the most popular frontend libraries in web development.

## Key Features

- **Component-Based**: Build encapsulated components managing their own state
- **Declarative**: Design simple views for each state
- **Virtual DOM**: Efficient updates and rendering
- **JSX**: JavaScript syntax extension for describing UI
- **Unidirectional Data Flow**: Props flow down, events bubble up
- **Hooks**: Functional components with state and lifecycle

## Architecture

### Component Types
- **Functional Components**: Modern, hook-based components
- **Class Components**: Traditional, lifecycle-based (legacy)

### Core Concepts
- **Props**: Read-only data passed to components
- **State**: Component-local data that can change
- **Context**: Share data across component tree
- **Refs**: Direct access to DOM elements
- **Portals**: Render children outside parent hierarchy

## Common Use Cases

### Web Applications
- Single Page Applications (SPAs)
- Progressive Web Apps (PWAs)
- E-commerce platforms
- Social media interfaces
- Dashboards and admin panels

### Mobile Development
- React Native for iOS and Android
- Cross-platform mobile apps
- Native mobile experiences

## Semantic Relationships

### Extends
- JavaScript ($.Technology:JavaScript) - Built on JavaScript

### Used By Occupations
- Frontend Developers ($.Occupation:15-1254.00)
- Software Developers ($.Occupation:15-1252.00)
- UI/UX Engineers

### Required For Tasks
- Build interactive user interfaces
- Create reusable UI components
- Implement state management
- Develop single-page applications

### Integrates With
- **Build Tools**: Vite, Webpack, Create React App
- **State Management**: Redux, Zustand, Jotai, Recoil
- **Routing**: React Router, TanStack Router
- **Styling**: Tailwind CSS, styled-components, CSS Modules
- **Testing**: Jest, React Testing Library, Vitest
- **Type Safety**: TypeScript ($.Technology:TypeScript)

### Technology Stacks
- **MERN**: MongoDB, Express, React, Node.js
- **T3**: TypeScript, tRPC, Tailwind, React
- **Next.js**: React framework for production

## Popular React Frameworks

### Meta-Frameworks
- **Next.js**: Production-ready React framework (Vercel)
- **Remix**: Full-stack web framework
- **Gatsby**: Static site generator

### Mobile
- **React Native**: Mobile app development
- **Expo**: React Native development platform

## Essential Hooks

```javascript
// State Management
const [count, setCount] = useState(0);

// Side Effects
useEffect(() => {
  document.title = `Count: ${count}`;
}, [count]);

// Context
const theme = useContext(ThemeContext);

// Refs
const inputRef = useRef(null);

// Memoization
const memoizedValue = useMemo(() => computeExpensive(a, b), [a, b]);
const memoizedCallback = useCallback(() => doSomething(a, b), [a, b]);

// Reducers
const [state, dispatch] = useReducer(reducer, initialState);
```

## Example Component

```jsx
import { useState, useEffect } from 'react';

function UserProfile({ userId }) {
  const [user, setUser] = useState(null);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    async function fetchUser() {
      try {
        const response = await fetch(`/api/users/${userId}`);
        const data = await response.json();
        setUser(data);
      } catch (error) {
        console.error('Failed to fetch user:', error);
      } finally {
        setLoading(false);
      }
    }

    fetchUser();
  }, [userId]);

  if (loading) return <div>Loading...</div>;
  if (!user) return <div>User not found</div>;

  return (
    <div className="user-profile">
      <h1>{user.name}</h1>
      <p>{user.email}</p>
      <p>Occupation: {user.occupation}</p>
    </div>
  );
}

export default UserProfile;
```

## Ecosystem

### State Management
- Redux Toolkit - Official Redux approach
- Zustand - Lightweight state management
- Jotai - Atomic state management
- Recoil - Facebook's state library
- MobX - Observable state

### UI Component Libraries
- Material-UI (MUI) - Material Design components
- Ant Design - Enterprise UI components
- Chakra UI - Accessible component library
- Radix UI - Unstyled, accessible components
- shadcn/ui - Re-usable components

### Data Fetching
- TanStack Query (React Query) - Data synchronization
- SWR - React hooks for data fetching
- Apollo Client - GraphQL client
- tRPC - End-to-end typesafe APIs

## React 18+ Features

- **Concurrent Rendering**: Improved performance
- **Automatic Batching**: Better state updates
- **Transitions**: Smooth UI updates
- **Suspense**: Better loading states
- **Server Components**: Server-side rendering improvements

## Resources

- Official Website: https://react.dev
- Documentation: https://react.dev/learn
- GitHub: https://github.com/facebook/react
- React Native: https://reactnative.dev
- Create React App: https://create-react-app.dev

## Related Technologies

- **Preact**: Lightweight React alternative
- **Solid.js**: Fine-grained reactivity
- **Vue.js**: Alternative framework
- **Angular**: Alternative framework
- **Svelte**: Compiler-based framework

---

**Data Source**: This page includes information from the O*NET 30.0 Database by the U.S. Department of Labor, Employment and Training Administration (USDOL/ETA). Used under the CC BY 4.0 license. O*NET® is a trademark of USDOL/ETA.

**Hot Technology**: ✅ Yes (Emerging/In-Demand)
**Package**: tech.org.ai
**Vendor**: Meta (Facebook)
