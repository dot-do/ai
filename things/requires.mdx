---
$type: Verb
$id: https://schema.org.ai/requires
$context: https://schema.org.ai
name: requires
description: A required dependency or prerequisite
status: public
license: MIT
source: schema.org
schemaVersion: '14.0'
category: Property
---

# requires

A required dependency or prerequisite

## Schema.org Reference

[requires](https://schema.org/requires) in Schema.org vocabulary.

## Usage

```typescript
import { $ } from 'sdk.do'

// Use this type in your code
const result = await db.query({
  subject: $.requires,
})
```
