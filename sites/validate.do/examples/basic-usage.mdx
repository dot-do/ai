---
$id: https://validate.do/examples/basic-usage
$type: HowTo
title: Basic Data Validation Usage
description: Simple examples of using validate.do
keywords: [validate, example, tutorial, basic, usage]
---

# Basic Data Validation Usage

Simple, practical examples of using validate.do.

## Example 1: Simple Operation

```typescript
import { validate } from 'validate.do'

// Basic usage
const result = await validate({
  data: 'example input',
})

console.log('Result:', result)
```

## Example 2: With Options

```typescript
import { validate } from 'validate.do'

// With configuration options
const result = await validate({
  data: 'example input',
  options: {
    format: 'json',
    validate: true,
  },
})

console.log('Configured result:', result)
```

## Example 3: Error Handling

```typescript
import { validate, ValidateError } from 'validate.do'

async function safeOperation(data: any) {
  try {
    const result = await validate({
      data,
      options: { strict: true },
    })
    return { success: true, result }
  } catch (error) {
    if (error instanceof ValidateError) {
      console.error('Data Validation error:', error.message)
      return { success: false, error: error.message }
    }
    throw error
  }
}

const result = await safeOperation('input data')
```

## Example 4: Batch Processing

```typescript
import { validate } from 'validate.do'

// Process multiple items
const items = ['item1', 'item2', 'item3']

const results = await Promise.all(items.map((item) => validate({ data: item })))

console.log('Batch results:', results)
```

## Example 5: With TypeScript

```typescript
import { validate } from 'validate.do'
import type { ValidateOptions, ValidateResult } from 'validate.do'

// Type-safe operation
const options: ValidateOptions = {
  data: 'typed input',
  validate: true,
  format: 'json',
}

const result: ValidateResult = await validate(options)
console.log('Typed result:', result)
```

## Complete Example

```typescript
import { validate } from 'validate.do'

async function main() {
  // Initialize
  console.log('Starting validate operation...')

  // Process data
  const result = await validate({
    data: 'example data',
    options: {
      format: 'json',
      validate: true,
      prettify: true,
    },
  })

  // Handle result
  if (result.success) {
    console.log('Success:', result.data)
  } else {
    console.error('Failed:', result.error)
  }

  return result
}

// Run
main().catch(console.error)
```

## Next Steps

- [Advanced Patterns](./advanced-patterns) - Complex workflows
- [Integration](./integration) - Service integration
- [Real-World Use Case](./real-world-use-case) - Production example

---

Part of the [validate.do](https://validate.do) documentation.
