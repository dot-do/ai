---
$id: https://transform.do/examples/basic-usage
$type: HowTo
title: Basic Data Transformation Usage
description: Simple examples of using transform.do
keywords: [transform, example, tutorial, basic, usage]
---

# Basic Data Transformation Usage

Simple, practical examples of using transform.do.

## Example 1: Simple Operation

```typescript
import { transform } from 'transform.do'

// Basic usage
const result = await transform({
  data: 'example input',
})

console.log('Result:', result)
```

## Example 2: With Options

```typescript
import { transform } from 'transform.do'

// With configuration options
const result = await transform({
  data: 'example input',
  options: {
    format: 'json',
    validate: true,
  },
})

console.log('Configured result:', result)
```

## Example 3: Error Handling

```typescript
import { transform, TransformError } from 'transform.do'

async function safeOperation(data: any) {
  try {
    const result = await transform({
      data,
      options: { strict: true },
    })
    return { success: true, result }
  } catch (error) {
    if (error instanceof TransformError) {
      console.error('Data Transformation error:', error.message)
      return { success: false, error: error.message }
    }
    throw error
  }
}

const result = await safeOperation('input data')
```

## Example 4: Batch Processing

```typescript
import { transform } from 'transform.do'

// Process multiple items
const items = ['item1', 'item2', 'item3']

const results = await Promise.all(items.map((item) => transform({ data: item })))

console.log('Batch results:', results)
```

## Example 5: With TypeScript

```typescript
import { transform } from 'transform.do'
import type { TransformOptions, TransformResult } from 'transform.do'

// Type-safe operation
const options: TransformOptions = {
  data: 'typed input',
  validate: true,
  format: 'json',
}

const result: TransformResult = await transform(options)
console.log('Typed result:', result)
```

## Complete Example

```typescript
import { transform } from 'transform.do'

async function main() {
  // Initialize
  console.log('Starting transform operation...')

  // Process data
  const result = await transform({
    data: 'example data',
    options: {
      format: 'json',
      validate: true,
      prettify: true,
    },
  })

  // Handle result
  if (result.success) {
    console.log('Success:', result.data)
  } else {
    console.error('Failed:', result.error)
  }

  return result
}

// Run
main().catch(console.error)
```

## Next Steps

- [Advanced Patterns](./advanced-patterns) - Complex workflows
- [Integration](./integration) - Service integration
- [Real-World Use Case](./real-world-use-case) - Production example

---

Part of the [transform.do](https://transform.do) documentation.
