---
$id: https://sandbox.do/docs/getting-started
$type: TechArticle
title: Getting Started with sandbox.do
description: Complete guide to setting up and using sandbox.do
keywords: [sandbox, execution, security, isolated, vm, code, eval, safe, getting-started, setup, tutorial]
---

# Getting Started with sandbox.do

Learn how to set up and use sandbox.do for code sandbox in your applications.

## Installation

Install sandbox.do using pnpm:

```bash
pnpm add sandbox.do
```

## Basic Setup

### 1. Import

```typescript
import { sandbox } from 'sandbox.do'
```

### 2. Configure

```typescript
// Optional configuration
const options = {
  // Configuration options
}
```

### 3. Use

```typescript
const result = await sandbox({
  // Your data
})

console.log('Result:', result)
```

## First Example

Here's a complete working example:

```typescript
import { sandbox } from 'sandbox.do'

async function example() {
  try {
    const result = await sandbox({
      data: 'example input',
      options: {
        format: 'json',
      },
    })

    console.log('Success:', result)
    return result
  } catch (error) {
    console.error('Error:', error)
    throw error
  }
}

// Run example
example()
```

## Basic Operations

### Operation 1

```typescript
const result = await sandbox.operation1(data)
```

### Operation 2

```typescript
const result = await sandbox.operation2(data, options)
```

### Operation 3

```typescript
const results = await sandbox.batch([data1, data2, data3])
```

## Configuration Options

sandbox.do supports various configuration options:

```typescript
const options = {
  // Option 1
  option1: 'value',

  // Option 2
  option2: true,

  // Option 3
  option3: {
    nested: 'value',
  },
}

const result = await sandbox(data, options)
```

## Error Handling

Handle errors appropriately:

```typescript
import { sandbox, SandboxError } from 'sandbox.do'

try {
  const result = await sandbox(data)
} catch (error) {
  if (error instanceof SandboxError) {
    console.error('Domain-specific error:', error.message)
  } else {
    console.error('Unexpected error:', error)
  }
}
```

## Next Steps

- [Architecture](./architecture) - Learn about system architecture
- [Best Practices](./best-practices) - Recommended patterns
- [API Reference](../api/reference) - Complete API documentation
- [Examples](../examples/basic-usage) - More code examples

## Common Patterns

### Pattern 1: Simple Usage

```typescript
const result = await sandbox(input)
```

### Pattern 2: With Options

```typescript
const result = await sandbox(input, {
  option: 'value',
})
```

### Pattern 3: Batch Processing

```typescript
const results = await Promise.all(items.map((item) => sandbox(item)))
```

## Integration with SDK

Use with sdk.do:

```typescript
import { sandbox } from 'sandbox.do'
import { db } from 'sdk.do'

// Fetch data from database
const data = await db.list($.Data)

// Process with sandbox.do
const results = await Promise.all(data.map((item) => sandbox(item)))
```

## Troubleshooting

### Issue: Installation fails

Verify you're using the correct package name:

```bash
pnpm add sandbox.do
```

### Issue: Import error

Ensure the import path is correct:

```typescript
import { sandbox } from 'sandbox.do'
```

## Learn More

- [Architecture](./architecture) - System design
- [Best Practices](./best-practices) - Recommended patterns
- [Troubleshooting](./troubleshooting) - Common issues

---

Part of the [sandbox.do](https://sandbox.do) documentation.
