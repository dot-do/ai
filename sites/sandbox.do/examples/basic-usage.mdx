---
$id: https://sandbox.do/examples/basic-usage
$type: HowTo
title: Basic Code Sandbox Usage
description: Simple examples of using sandbox.do
keywords: [sandbox, example, tutorial, basic, usage]
---

# Basic Code Sandbox Usage

Simple, practical examples of using sandbox.do.

## Example 1: Simple Operation

```typescript
import { sandbox } from 'sandbox.do'

// Basic usage
const result = await sandbox({
  data: 'example input',
})

console.log('Result:', result)
```

## Example 2: With Options

```typescript
import { sandbox } from 'sandbox.do'

// With configuration options
const result = await sandbox({
  data: 'example input',
  options: {
    format: 'json',
    validate: true,
  },
})

console.log('Configured result:', result)
```

## Example 3: Error Handling

```typescript
import { sandbox, SandboxError } from 'sandbox.do'

async function safeOperation(data: any) {
  try {
    const result = await sandbox({
      data,
      options: { strict: true },
    })
    return { success: true, result }
  } catch (error) {
    if (error instanceof SandboxError) {
      console.error('Code Sandbox error:', error.message)
      return { success: false, error: error.message }
    }
    throw error
  }
}

const result = await safeOperation('input data')
```

## Example 4: Batch Processing

```typescript
import { sandbox } from 'sandbox.do'

// Process multiple items
const items = ['item1', 'item2', 'item3']

const results = await Promise.all(items.map((item) => sandbox({ data: item })))

console.log('Batch results:', results)
```

## Example 5: With TypeScript

```typescript
import { sandbox } from 'sandbox.do'
import type { SandboxOptions, SandboxResult } from 'sandbox.do'

// Type-safe operation
const options: SandboxOptions = {
  data: 'typed input',
  validate: true,
  format: 'json',
}

const result: SandboxResult = await sandbox(options)
console.log('Typed result:', result)
```

## Complete Example

```typescript
import { sandbox } from 'sandbox.do'

async function main() {
  // Initialize
  console.log('Starting sandbox operation...')

  // Process data
  const result = await sandbox({
    data: 'example data',
    options: {
      format: 'json',
      validate: true,
      prettify: true,
    },
  })

  // Handle result
  if (result.success) {
    console.log('Success:', result.data)
  } else {
    console.error('Failed:', result.error)
  }

  return result
}

// Run
main().catch(console.error)
```

## Next Steps

- [Advanced Patterns](./advanced-patterns) - Complex workflows
- [Integration](./integration) - Service integration
- [Real-World Use Case](./real-world-use-case) - Production example

---

Part of the [sandbox.do](https://sandbox.do) documentation.
