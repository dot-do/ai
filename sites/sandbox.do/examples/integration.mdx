---
$id: https://sandbox.do/examples/integration
$type: HowTo
title: Code Sandbox Integration Examples
description: Integrate sandbox.do with other services and platforms
keywords: [sandbox, integration, sdk, events, api]
---

# Code Sandbox Integration Examples

Learn how to integrate sandbox.do with other `.do` platform services.

## Integration with SDK

```typescript
import { sandbox } from 'sandbox.do'
import { db, ai } from 'sdk.do'

// Fetch data from database
const data = await db.list($.Data)

// Process with sandbox.do
const results = await sandbox(data)

// Use AI to analyze
const analysis = await ai.generate('analyze', { results })
```

## Integration with Events

```typescript
import { sandbox } from 'sandbox.do'
import { on, send } from 'sdk.do'

// Process on event
on($.Data.received, async (data) => {
  const result = await sandbox(data)
  send($.Data.processed, result)
})
```

## Integration with Workflows

```typescript
import { sandbox } from 'sandbox.do'
import { every } from 'sdk.do'

// Scheduled processing
every($.Daily, async () => {
  const data = await fetchDailyData()
  const results = await sandbox(data)
  await saveResults(results)
})
```

## Integration with Queue

```typescript
import { sandbox } from 'sandbox.do'
import { queue } from 'queue.do'

const processQueue = queue($.Data.process)

processQueue.process(async (job) => {
  const result = await sandbox(job.data)
  return result
})
```

## Integration with External APIs

```typescript
import { sandbox } from 'sandbox.do'
import { api } from 'sdk.do'

// Fetch from external API
const external = await api('https://api.example.com/data')

// Process with sandbox.do
const result = await sandbox(external)

// Send back
await api('https://api.example.com/results', {
  method: 'POST',
  body: JSON.stringify(result),
})
```

## Learn More

- [Basic Usage](./basic-usage) - Simple examples
- [Advanced Patterns](./advanced-patterns) - Complex workflows

---

Part of the [sandbox.do](https://sandbox.do) documentation.
