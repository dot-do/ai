---
\$id: https://evals.do/examples/real-world-use-case
\$type: HowTo
title: Real-World Use Case - evals.do
description: Complete production implementation
keywords: [real-world, production, complete example]
author:
  \$type: Organization
  name: .do Platform
---

# Real-World Use Case

Complete production implementation example.

## Overview

This example demonstrates a full production setup.

## Implementation

\`\`\`typescript
import \$, { evals, db, on, every } from 'sdk.do'

// Setup
const config = {
// Configuration
}

// Run periodically
await every($.Daily, async () => {
  const result = await evals.run(config)
  await db.create($.Result, result)
})

// Handle events
on($.Event, async (event) => {
await evals.run({ event })
})
\`\`\`

## Results

This implementation provides comprehensive coverage of production scenarios.

---

This completes the real-world examples for evals.do.
