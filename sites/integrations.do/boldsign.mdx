---
$id: https://integrations.do/boldsign
$type: Integration
name: Boldsign
service: boldsign
description: BoldSign delivers eSignature solutions, letting organizations create, send, and manage legally binding documents through streamlined digital signing workflows
category: storage
sdkBased: false
baseUrl: https://api.boldsign.com

auth:
  type: oauth2
  authorizationUrl: https://account.boldsign.com/connect/authorize
  tokenUrl: https://account.boldsign.com/connect/token
  scopes:
    - openid profile email offline_access BoldSign.Documents.All

resources:
  - name: Action
    plural: Actions
    description: Execute Boldsign actions
    operations:
      - name: execute
        method: POST
        path: /
        params:
          - name: action
            type: string
            required: true
            description: Action name to execute
          - name: parameters
            type: object
            required: false
            description: Action parameters
        returns: object

webhooks:
  enabled: false
  events: []

errors:
  mapping:
    - code: 401
      type: authentication
      statusCode: 401
      message: Unauthorized
      retryable: false
    - code: 429
      type: rate_limit
      statusCode: 429
      message: Rate limit exceeded
      retryable: true
  retryable: [429, 500]
  nonRetryable: [400, 401, 403, 404]

tests:
  enabled: true
  cleanup: false
  scenarios:
    - name: Basic Action Execution
      description: Test basic action execution
      steps:
        - action: custom
          resource: Action

docs:
  homepage: https://boldsign.com/
  apiDocs: https://boldsign.com/
---

# Boldsign Integration

BoldSign delivers eSignature solutions, letting organizations create, send, and manage legally binding documents through streamlined digital signing workflows

## Quick Start

```typescript
import { BoldsignClient } from '@dotdo/sdk.do/integrations/boldsign'

const boldsign = new BoldsignClient({
  apiKey: process.env.BOLDSIGN_API_KEY,
})

const result = await boldsign.action.execute({
  action: 'example_action',
  parameters: {},
})
```

## Actions

Boldsign provides 0 actions through the Composio API.

## License

MIT
