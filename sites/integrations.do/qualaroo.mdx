---
$id: https://integrations.do/qualaroo
$type: Integration
name: Qualaroo
service: qualaroo
description: Qualaroo offers on-site surveys and user feedback tools, helping businesses understand visitor behavior, gather insights, and optimize customer experiences
category: crm
sdkBased: false
baseUrl: https://api.qualaroo.com

auth:
  type: api-key
  location: header
  headerName: Authorization
  scheme: Bearer

resources:
  - name: Action
    plural: Actions
    description: Execute Qualaroo actions
    operations:
      - name: execute
        method: POST
        path: /
        params:
          - name: action
            type: string
            required: true
            description: Action name to execute
          - name: parameters
            type: object
            required: false
            description: Action parameters
        returns: object

webhooks:
  enabled: false
  events: []

errors:
  mapping:
    - code: 401
      type: authentication
      statusCode: 401
      message: Unauthorized
      retryable: false
    - code: 429
      type: rate_limit
      statusCode: 429
      message: Rate limit exceeded
      retryable: true
  retryable: [429, 500]
  nonRetryable: [400, 401, 403, 404]

tests:
  enabled: true
  cleanup: false
  scenarios:
    - name: Basic Action Execution
      description: Test basic action execution
      steps:
        - action: custom
          resource: Action

docs:
  homepage: https://qualaroo.com/
  apiDocs: https://qualaroo.com/
---

# Qualaroo Integration

Qualaroo offers on-site surveys and user feedback tools, helping businesses understand visitor behavior, gather insights, and optimize customer experiences

## Quick Start

```typescript
import { QualarooClient } from '@dotdo/sdk.do/integrations/qualaroo'

const qualaroo = new QualarooClient({
  apiKey: process.env.QUALAROO_API_KEY,
})

const result = await qualaroo.action.execute({
  action: 'example_action',
  parameters: {},
})
```

## Actions

Qualaroo provides 0 actions through the Composio API.

## License

MIT
