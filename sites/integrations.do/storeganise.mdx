---
$id: https://integrations.do/storeganise
$type: Integration
name: Storeganise
service: storeganise
description: Storeganise provides modern, cloud-based management software for valet and self-storage businesses, offering features like contactless online bookings, automated billing, and comprehensive APIs for integrations.
category: productivity
sdkBased: false
baseUrl: https://api.storeganise.com

auth:
  type: api-key
  location: header
  headerName: Authorization
  scheme: Bearer

resources:
  - name: Action
    plural: Actions
    description: Execute Storeganise actions
    operations:
      - name: execute
        method: POST
        path: /
        params:
          - name: action
            type: string
            required: true
            description: Action name to execute
          - name: parameters
            type: object
            required: false
            description: Action parameters
        returns: object

webhooks:
  enabled: false
  events: []

errors:
  mapping:
    - code: 401
      type: authentication
      statusCode: 401
      message: Unauthorized
      retryable: false
    - code: 429
      type: rate_limit
      statusCode: 429
      message: Rate limit exceeded
      retryable: true
  retryable: [429, 500]
  nonRetryable: [400, 401, 403, 404]

tests:
  enabled: true
  cleanup: false
  scenarios:
    - name: Basic Action Execution
      description: Test basic action execution
      steps:
        - action: custom
          resource: Action

docs:
  homepage: https://storeganise.com/
  apiDocs: https://storeganise.com/
---

# Storeganise Integration

Storeganise provides modern, cloud-based management software for valet and self-storage businesses, offering features like contactless online bookings, automated billing, and comprehensive APIs for integrations.

## Quick Start

```typescript
import { StoreganiseClient } from '@dotdo/sdk.do/integrations/storeganise'

const storeganise = new StoreganiseClient({
  apiKey: process.env.STOREGANISE_API_KEY,
})

const result = await storeganise.action.execute({
  action: 'example_action',
  parameters: {},
})
```

## Actions

Storeganise provides 3 actions through the Composio API.

## License

MIT
