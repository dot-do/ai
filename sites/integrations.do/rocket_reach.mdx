---
$id: https://integrations.do/rocket_reach
$type: Integration
name: Rocket reach
service: rocket_reach
description: RocketReach locates and verifies professional contact details, helping sales and recruiters reach leads and candidates more efficiently
category: crm
sdkBased: false
baseUrl: https://api.rocket_reach.com

auth:
  type: api-key
  location: header
  headerName: Authorization
  scheme: Bearer

resources:
  - name: Action
    plural: Actions
    description: Execute Rocket reach actions
    operations:
      - name: execute
        method: POST
        path: /
        params:
          - name: action
            type: string
            required: true
            description: Action name to execute
          - name: parameters
            type: object
            required: false
            description: Action parameters
        returns: object

webhooks:
  enabled: false
  events: []

errors:
  mapping:
    - code: 401
      type: authentication
      statusCode: 401
      message: Unauthorized
      retryable: false
    - code: 429
      type: rate_limit
      statusCode: 429
      message: Rate limit exceeded
      retryable: true
  retryable: [429, 500]
  nonRetryable: [400, 401, 403, 404]

tests:
  enabled: true
  cleanup: false
  scenarios:
    - name: Basic Action Execution
      description: Test basic action execution
      steps:
        - action: custom
          resource: Action

docs:
  homepage: https://www.rocketreach.co
  apiDocs: https://www.rocketreach.co
---

# Rocket reach Integration

RocketReach locates and verifies professional contact details, helping sales and recruiters reach leads and candidates more efficiently

## Quick Start

```typescript
import { RocketreachClient } from '@dotdo/sdk.do/integrations/rocket_reach'

const rocket_reach = new RocketreachClient({
  apiKey: process.env.ROCKET_REACH_API_KEY,
})

const result = await rocket_reach.action.execute({
  action: 'example_action',
  parameters: {},
})
```

## Actions

Rocket reach provides 0 actions through the Composio API.

## License

MIT
