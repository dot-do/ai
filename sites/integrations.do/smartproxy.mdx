---
$id: https://integrations.do/smartproxy
$type: Integration
name: Smartproxy
service: smartproxy
description: Smartproxy offers a comprehensive suite of proxy and web scraping solutions, providing users with access to a vast network of residential, datacenter, ISP, and mobile proxies, along with scraping APIs for efficient data collection.
category: productivity
sdkBased: false
baseUrl: https://api.smartproxy.com

auth:
  type: api-key
  location: header
  headerName: Authorization
  scheme: Bearer

resources:
  - name: Action
    plural: Actions
    description: Execute Smartproxy actions
    operations:
      - name: execute
        method: POST
        path: /
        params:
          - name: action
            type: string
            required: true
            description: Action name to execute
          - name: parameters
            type: object
            required: false
            description: Action parameters
        returns: object

webhooks:
  enabled: false
  events: []

errors:
  mapping:
    - code: 401
      type: authentication
      statusCode: 401
      message: Unauthorized
      retryable: false
    - code: 429
      type: rate_limit
      statusCode: 429
      message: Rate limit exceeded
      retryable: true
  retryable: [429, 500]
  nonRetryable: [400, 401, 403, 404]

tests:
  enabled: true
  cleanup: false
  scenarios:
    - name: Basic Action Execution
      description: Test basic action execution
      steps:
        - action: custom
          resource: Action

docs:
  homepage: https://smartproxy.com/
  apiDocs: https://smartproxy.com/
---

# Smartproxy Integration

Smartproxy offers a comprehensive suite of proxy and web scraping solutions, providing users with access to a vast network of residential, datacenter, ISP, and mobile proxies, along with scraping APIs for efficient data collection.

## Quick Start

```typescript
import { SmartproxyClient } from '@dotdo/sdk.do/integrations/smartproxy'

const smartproxy = new SmartproxyClient({
  apiKey: process.env.SMARTPROXY_API_KEY,
})

const result = await smartproxy.action.execute({
  action: 'example_action',
  parameters: {},
})
```

## Actions

Smartproxy provides 2 actions through the Composio API.

## License

MIT
