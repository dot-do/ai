---
$id: https://integrations.do/supportivekoala
$type: Integration
name: Supportivekoala
service: supportivekoala
description: Supportivekoala is a platform that automates image generation using customizable templates, enabling users to create visual content programmatically through API integration.
category: productivity
sdkBased: false
baseUrl: https://api.supportivekoala.com

auth:
  type: api-key
  location: header
  headerName: Authorization
  scheme: Bearer

resources:
  - name: Action
    plural: Actions
    description: Execute Supportivekoala actions
    operations:
      - name: execute
        method: POST
        path: /
        params:
          - name: action
            type: string
            required: true
            description: Action name to execute
          - name: parameters
            type: object
            required: false
            description: Action parameters
        returns: object

webhooks:
  enabled: false
  events: []

errors:
  mapping:
    - code: 401
      type: authentication
      statusCode: 401
      message: Unauthorized
      retryable: false
    - code: 429
      type: rate_limit
      statusCode: 429
      message: Rate limit exceeded
      retryable: true
  retryable: [429, 500]
  nonRetryable: [400, 401, 403, 404]

tests:
  enabled: true
  cleanup: false
  scenarios:
    - name: Basic Action Execution
      description: Test basic action execution
      steps:
        - action: custom
          resource: Action

docs:
  homepage: https://supportivekoala.com/
  apiDocs: https://supportivekoala.com/
---

# Supportivekoala Integration

Supportivekoala is a platform that automates image generation using customizable templates, enabling users to create visual content programmatically through API integration.

## Quick Start

```typescript
import { SupportivekoalaClient } from '@dotdo/sdk.do/integrations/supportivekoala'

const supportivekoala = new SupportivekoalaClient({
  apiKey: process.env.SUPPORTIVEKOALA_API_KEY,
})

const result = await supportivekoala.action.execute({
  action: 'example_action',
  parameters: {},
})
```

## Actions

Supportivekoala provides 6 actions through the Composio API.

## License

MIT
