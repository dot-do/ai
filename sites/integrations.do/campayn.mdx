---
$id: https://integrations.do/campayn
$type: Integration
name: Campayn
service: campayn
description: Campayn is an email marketing platform that allows users to create, send, and manage email campaigns.
category: productivity
sdkBased: false
baseUrl: https://api.campayn.com

auth:
  type: api-key
  location: header
  headerName: Authorization
  scheme: Bearer

resources:
  - name: Action
    plural: Actions
    description: Execute Campayn actions
    operations:
      - name: execute
        method: POST
        path: /
        params:
          - name: action
            type: string
            required: true
            description: Action name to execute
          - name: parameters
            type: object
            required: false
            description: Action parameters
        returns: object

webhooks:
  enabled: false
  events: []

errors:
  mapping:
    - code: 401
      type: authentication
      statusCode: 401
      message: Unauthorized
      retryable: false
    - code: 429
      type: rate_limit
      statusCode: 429
      message: Rate limit exceeded
      retryable: true
  retryable: [429, 500]
  nonRetryable: [400, 401, 403, 404]

tests:
  enabled: true
  cleanup: false
  scenarios:
    - name: Basic Action Execution
      description: Test basic action execution
      steps:
        - action: custom
          resource: Action

docs:
  homepage: https://www.campayn.com/
  apiDocs: https://www.campayn.com/
---

# Campayn Integration

Campayn is an email marketing platform that allows users to create, send, and manage email campaigns.

## Quick Start

```typescript
import { CampaynClient } from '@dotdo/sdk.do/integrations/campayn'

const campayn = new CampaynClient({
  apiKey: process.env.CAMPAYN_API_KEY,
})

const result = await campayn.action.execute({
  action: 'example_action',
  parameters: {},
})
```

## Actions

Campayn provides 14 actions through the Composio API.

## License

MIT
