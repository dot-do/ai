---
$id: https://integrations.do/eventee
$type: Integration
name: Eventee
service: eventee
description: Eventee is an intuitive mobile and web app that enhances attendee experience and engagement at in-person, virtual, or hybrid events.
category: productivity
sdkBased: false
baseUrl: https://api.eventee.com

auth:
  type: api-key
  location: header
  headerName: Authorization
  scheme: Bearer

resources:
  - name: Action
    plural: Actions
    description: Execute Eventee actions
    operations:
      - name: execute
        method: POST
        path: /
        params:
          - name: action
            type: string
            required: true
            description: Action name to execute
          - name: parameters
            type: object
            required: false
            description: Action parameters
        returns: object

webhooks:
  enabled: false
  events: []

errors:
  mapping:
    - code: 401
      type: authentication
      statusCode: 401
      message: Unauthorized
      retryable: false
    - code: 429
      type: rate_limit
      statusCode: 429
      message: Rate limit exceeded
      retryable: true
  retryable: [429, 500]
  nonRetryable: [400, 401, 403, 404]

tests:
  enabled: true
  cleanup: false
  scenarios:
    - name: Basic Action Execution
      description: Test basic action execution
      steps:
        - action: custom
          resource: Action

docs:
  homepage: https://eventee.co/
  apiDocs: https://eventee.co/
---

# Eventee Integration

Eventee is an intuitive mobile and web app that enhances attendee experience and engagement at in-person, virtual, or hybrid events.

## Quick Start

```typescript
import { EventeeClient } from '@dotdo/sdk.do/integrations/eventee'

const eventee = new EventeeClient({
  apiKey: process.env.EVENTEE_API_KEY,
})

const result = await eventee.action.execute({
  action: 'example_action',
  parameters: {},
})
```

## Actions

Eventee provides 3 actions through the Composio API.

## License

MIT
