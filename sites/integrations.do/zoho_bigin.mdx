---
$id: https://integrations.do/zoho_bigin
$type: Integration
name: Zoho bigin
service: zoho_bigin
description: Zoho Bigin is a simplified CRM solution from Zoho tailored for small businesses, focusing on pipeline tracking and relationship management
category: crm
sdkBased: false
baseUrl: https://api.zoho_bigin.com

auth:
  type: oauth2
  authorizationUrl: https://accounts.zoho.{{suffix_one}}/oauth/v2/auth
  tokenUrl: https://accounts.zoho.{{suffix_one}}/oauth/v2/token
  scopes:
    - ZohoBigin.modules.ALL
    - ZohoBigin.users.ALL

resources:
  - name: Action
    plural: Actions
    description: Execute Zoho bigin actions
    operations:
      - name: execute
        method: POST
        path: /
        params:
          - name: action
            type: string
            required: true
            description: Action name to execute
          - name: parameters
            type: object
            required: false
            description: Action parameters
        returns: object

webhooks:
  enabled: false
  events: []

errors:
  mapping:
    - code: 401
      type: authentication
      statusCode: 401
      message: Unauthorized
      retryable: false
    - code: 429
      type: rate_limit
      statusCode: 429
      message: Rate limit exceeded
      retryable: true
  retryable: [429, 500]
  nonRetryable: [400, 401, 403, 404]

tests:
  enabled: true
  cleanup: false
  scenarios:
    - name: Basic Action Execution
      description: Test basic action execution
      steps:
        - action: custom
          resource: Action

docs:
  homepage: https://www.bigin.com
  apiDocs: https://www.bigin.com
---

# Zoho bigin Integration

Zoho Bigin is a simplified CRM solution from Zoho tailored for small businesses, focusing on pipeline tracking and relationship management

## Quick Start

```typescript
import { ZohobiginClient } from '@dotdo/sdk.do/integrations/zoho_bigin'

const zoho_bigin = new ZohobiginClient({
  apiKey: process.env.ZOHO_BIGIN_API_KEY,
})

const result = await zoho_bigin.action.execute({
  action: 'example_action',
  parameters: {},
})
```

## Actions

Zoho bigin provides 12 actions through the Composio API.

## License

MIT
