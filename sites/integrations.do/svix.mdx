---
$id: https://integrations.do/svix
$type: Integration
name: Svix
service: svix
description: Svix is an enterprise-ready webhooks service that enables developers to send webhooks reliably and securely.
category: productivity
sdkBased: false
baseUrl: https://api.svix.com

auth:
  type: api-key
  location: header
  headerName: Authorization
  scheme: Bearer

resources:
  - name: Action
    plural: Actions
    description: Execute Svix actions
    operations:
      - name: execute
        method: POST
        path: /
        params:
          - name: action
            type: string
            required: true
            description: Action name to execute
          - name: parameters
            type: object
            required: false
            description: Action parameters
        returns: object

webhooks:
  enabled: false
  events: []

errors:
  mapping:
    - code: 401
      type: authentication
      statusCode: 401
      message: Unauthorized
      retryable: false
    - code: 429
      type: rate_limit
      statusCode: 429
      message: Rate limit exceeded
      retryable: true
  retryable: [429, 500]
  nonRetryable: [400, 401, 403, 404]

tests:
  enabled: true
  cleanup: false
  scenarios:
    - name: Basic Action Execution
      description: Test basic action execution
      steps:
        - action: custom
          resource: Action

docs:
  homepage: https://www.svix.com/
  apiDocs: https://www.svix.com/
---

# Svix Integration

Svix is an enterprise-ready webhooks service that enables developers to send webhooks reliably and securely.

## Quick Start

```typescript
import { SvixClient } from '@dotdo/sdk.do/integrations/svix'

const svix = new SvixClient({
  apiKey: process.env.SVIX_API_KEY,
})

const result = await svix.action.execute({
  action: 'example_action',
  parameters: {},
})
```

## Actions

Svix provides 37 actions through the Composio API.

## License

MIT
