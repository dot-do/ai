---
$id: https://integrations.do/bamboohr
$type: Integration
name: Bamboohr
service: bamboohr
description: BambooHR is an American technology company that provides human resources software as a service.
category: productivity
sdkBased: false
baseUrl: https://api.bamboohr.com

auth:
  type: api-key
  location: header
  headerName: Authorization
  scheme: Basic

resources:
  - name: Action
    plural: Actions
    description: Execute Bamboohr actions
    operations:
      - name: execute
        method: POST
        path: /
        params:
          - name: action
            type: string
            required: true
            description: Action name to execute
          - name: parameters
            type: object
            required: false
            description: Action parameters
        returns: object

webhooks:
  enabled: false
  events: []

errors:
  mapping:
    - code: 401
      type: authentication
      statusCode: 401
      message: Unauthorized
      retryable: false
    - code: 429
      type: rate_limit
      statusCode: 429
      message: Rate limit exceeded
      retryable: true
  retryable: [429, 500]
  nonRetryable: [400, 401, 403, 404]

tests:
  enabled: true
  cleanup: false
  scenarios:
    - name: Basic Action Execution
      description: Test basic action execution
      steps:
        - action: custom
          resource: Action

docs:
  homepage: https://www.bamboohr.com
  apiDocs: https://www.bamboohr.com
---

# Bamboohr Integration

BambooHR is an American technology company that provides human resources software as a service.

## Quick Start

```typescript
import { BamboohrClient } from '@dotdo/sdk.do/integrations/bamboohr'

const bamboohr = new BamboohrClient({
  apiKey: process.env.BAMBOOHR_API_KEY,
})

const result = await bamboohr.action.execute({
  action: 'example_action',
  parameters: {},
})
```

## Actions

Bamboohr provides 159 actions through the Composio API.

## License

MIT
