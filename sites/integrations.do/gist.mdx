---
$id: https://integrations.do/gist
$type: Integration
name: Gist
service: gist
description: GitHub Gist is a service provided by GitHub that allows users to share code snippets, notes, and other text-based content. It supports both public and private gists, enabling easy sharing and collaboration.
category: productivity
sdkBased: false
baseUrl: https://api.gist.com

auth:
  type: api-key
  location: header
  headerName: Authorization
  scheme: Bearer

resources:
  - name: Action
    plural: Actions
    description: Execute Gist actions
    operations:
      - name: execute
        method: POST
        path: /
        params:
          - name: action
            type: string
            required: true
            description: Action name to execute
          - name: parameters
            type: object
            required: false
            description: Action parameters
        returns: object

webhooks:
  enabled: false
  events: []

errors:
  mapping:
    - code: 401
      type: authentication
      statusCode: 401
      message: Unauthorized
      retryable: false
    - code: 429
      type: rate_limit
      statusCode: 429
      message: Rate limit exceeded
      retryable: true
  retryable: [429, 500]
  nonRetryable: [400, 401, 403, 404]

tests:
  enabled: true
  cleanup: false
  scenarios:
    - name: Basic Action Execution
      description: Test basic action execution
      steps:
        - action: custom
          resource: Action

docs:
  homepage: https://gist.github.com/
  apiDocs: https://gist.github.com/
---

# Gist Integration

GitHub Gist is a service provided by GitHub that allows users to share code snippets, notes, and other text-based content. It supports both public and private gists, enabling easy sharing and collaboration.

## Quick Start

```typescript
import { GistClient } from '@dotdo/sdk.do/integrations/gist'

const gist = new GistClient({
  apiKey: process.env.GIST_API_KEY,
})

const result = await gist.action.execute({
  action: 'example_action',
  parameters: {},
})
```

## Actions

Gist provides 13 actions through the Composio API.

## License

MIT
