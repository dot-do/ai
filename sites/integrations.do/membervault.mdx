---
$id: https://integrations.do/membervault
$type: Integration
name: Membervault
service: membervault
description: MemberVault is a Relationship Marketing Platform that allows users to host courses, memberships, and other digital products in one place.
category: productivity
sdkBased: false
baseUrl: https://api.membervault.com

auth:
  type: api-key
  location: header
  headerName: Authorization
  scheme: Bearer

resources:
  - name: Action
    plural: Actions
    description: Execute Membervault actions
    operations:
      - name: execute
        method: POST
        path: /
        params:
          - name: action
            type: string
            required: true
            description: Action name to execute
          - name: parameters
            type: object
            required: false
            description: Action parameters
        returns: object

webhooks:
  enabled: false
  events: []

errors:
  mapping:
    - code: 401
      type: authentication
      statusCode: 401
      message: Unauthorized
      retryable: false
    - code: 429
      type: rate_limit
      statusCode: 429
      message: Rate limit exceeded
      retryable: true
  retryable: [429, 500]
  nonRetryable: [400, 401, 403, 404]

tests:
  enabled: true
  cleanup: false
  scenarios:
    - name: Basic Action Execution
      description: Test basic action execution
      steps:
        - action: custom
          resource: Action

docs:
  homepage: https://membervault.co/
  apiDocs: https://membervault.co/
---

# Membervault Integration

MemberVault is a Relationship Marketing Platform that allows users to host courses, memberships, and other digital products in one place.

## Quick Start

```typescript
import { MembervaultClient } from '@dotdo/sdk.do/integrations/membervault'

const membervault = new MembervaultClient({
  apiKey: process.env.MEMBERVAULT_API_KEY,
})

const result = await membervault.action.execute({
  action: 'example_action',
  parameters: {},
})
```

## Actions

Membervault provides 3 actions through the Composio API.

## License

MIT
