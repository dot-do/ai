---
$id: https://integrations.do/callingly
$type: Integration
name: Callingly
service: callingly
description: Callingly is a lead response management software that automates immediate call and text follow-ups to new leads, integrating seamlessly with various CRMs and lead sources to enhance sales team responsiveness and conversion rates.
category: productivity
sdkBased: false
baseUrl: https://api.callingly.com

auth:
  type: api-key
  location: header
  headerName: Authorization
  scheme: Bearer

resources:
  - name: Action
    plural: Actions
    description: Execute Callingly actions
    operations:
      - name: execute
        method: POST
        path: /
        params:
          - name: action
            type: string
            required: true
            description: Action name to execute
          - name: parameters
            type: object
            required: false
            description: Action parameters
        returns: object

webhooks:
  enabled: false
  events: []

errors:
  mapping:
    - code: 401
      type: authentication
      statusCode: 401
      message: Unauthorized
      retryable: false
    - code: 429
      type: rate_limit
      statusCode: 429
      message: Rate limit exceeded
      retryable: true
  retryable: [429, 500]
  nonRetryable: [400, 401, 403, 404]

tests:
  enabled: true
  cleanup: false
  scenarios:
    - name: Basic Action Execution
      description: Test basic action execution
      steps:
        - action: custom
          resource: Action

docs:
  homepage: https://callingly.com/
  apiDocs: https://callingly.com/
---

# Callingly Integration

Callingly is a lead response management software that automates immediate call and text follow-ups to new leads, integrating seamlessly with various CRMs and lead sources to enhance sales team responsiveness and conversion rates.

## Quick Start

```typescript
import { CallinglyClient } from '@dotdo/sdk.do/integrations/callingly'

const callingly = new CallinglyClient({
  apiKey: process.env.CALLINGLY_API_KEY,
})

const result = await callingly.action.execute({
  action: 'example_action',
  parameters: {},
})
```

## Actions

Callingly provides 27 actions through the Composio API.

## License

MIT
