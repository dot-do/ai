---
$id: https://integrations.do/tomba
$type: Integration
name: Tomba
service: tomba
description: Tomba is an Email Finder for B2B sales and email marketing.
category: productivity
sdkBased: false
baseUrl: https://api.tomba.com

auth:
  type: api-key
  location: header
  headerName: Authorization
  scheme: Bearer

resources:
  - name: Action
    plural: Actions
    description: Execute Tomba actions
    operations:
      - name: execute
        method: POST
        path: /
        params:
          - name: action
            type: string
            required: true
            description: Action name to execute
          - name: parameters
            type: object
            required: false
            description: Action parameters
        returns: object

webhooks:
  enabled: false
  events: []

errors:
  mapping:
    - code: 401
      type: authentication
      statusCode: 401
      message: Unauthorized
      retryable: false
    - code: 429
      type: rate_limit
      statusCode: 429
      message: Rate limit exceeded
      retryable: true
  retryable: [429, 500]
  nonRetryable: [400, 401, 403, 404]

tests:
  enabled: true
  cleanup: false
  scenarios:
    - name: Basic Action Execution
      description: Test basic action execution
      steps:
        - action: custom
          resource: Action

docs:
  homepage: https://app.tomba.io/auth/register
  apiDocs: https://app.tomba.io/auth/register
---

# Tomba Integration

Tomba is an Email Finder for B2B sales and email marketing.

## Quick Start

```typescript
import { TombaClient } from '@dotdo/sdk.do/integrations/tomba'

const tomba = new TombaClient({
  apiKey: process.env.TOMBA_API_KEY,
})

const result = await tomba.action.execute({
  action: 'example_action',
  parameters: {},
})
```

## Actions

Tomba provides 10 actions through the Composio API.

## License

MIT
