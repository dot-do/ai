---
$id: https://schedule.do/examples/basic-usage
$type: HowTo
title: Basic Task Scheduling Usage
description: Simple examples of using schedule.do
keywords: [schedule, example, tutorial, basic, usage]
---

# Basic Task Scheduling Usage

Simple, practical examples of using schedule.do.

## Example 1: Simple Operation

```typescript
import { schedule } from 'schedule.do'

// Basic usage
const result = await schedule({
  data: 'example input',
})

console.log('Result:', result)
```

## Example 2: With Options

```typescript
import { schedule } from 'schedule.do'

// With configuration options
const result = await schedule({
  data: 'example input',
  options: {
    format: 'json',
    validate: true,
  },
})

console.log('Configured result:', result)
```

## Example 3: Error Handling

```typescript
import { schedule, ScheduleError } from 'schedule.do'

async function safeOperation(data: any) {
  try {
    const result = await schedule({
      data,
      options: { strict: true },
    })
    return { success: true, result }
  } catch (error) {
    if (error instanceof ScheduleError) {
      console.error('Task Scheduling error:', error.message)
      return { success: false, error: error.message }
    }
    throw error
  }
}

const result = await safeOperation('input data')
```

## Example 4: Batch Processing

```typescript
import { schedule } from 'schedule.do'

// Process multiple items
const items = ['item1', 'item2', 'item3']

const results = await Promise.all(items.map((item) => schedule({ data: item })))

console.log('Batch results:', results)
```

## Example 5: With TypeScript

```typescript
import { schedule } from 'schedule.do'
import type { ScheduleOptions, ScheduleResult } from 'schedule.do'

// Type-safe operation
const options: ScheduleOptions = {
  data: 'typed input',
  validate: true,
  format: 'json',
}

const result: ScheduleResult = await schedule(options)
console.log('Typed result:', result)
```

## Complete Example

```typescript
import { schedule } from 'schedule.do'

async function main() {
  // Initialize
  console.log('Starting schedule operation...')

  // Process data
  const result = await schedule({
    data: 'example data',
    options: {
      format: 'json',
      validate: true,
      prettify: true,
    },
  })

  // Handle result
  if (result.success) {
    console.log('Success:', result.data)
  } else {
    console.error('Failed:', result.error)
  }

  return result
}

// Run
main().catch(console.error)
```

## Next Steps

- [Advanced Patterns](./advanced-patterns) - Complex workflows
- [Integration](./integration) - Service integration
- [Real-World Use Case](./real-world-use-case) - Production example

---

Part of the [schedule.do](https://schedule.do) documentation.
