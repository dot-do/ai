---
$id: https://schedule.do/docs/getting-started
$type: TechArticle
title: Getting Started with schedule.do
description: Complete guide to setting up and using schedule.do
keywords: [schedule, cron, timer, interval, task, workflow, recurring, calendar, getting-started, setup, tutorial]
---

# Getting Started with schedule.do

Learn how to set up and use schedule.do for task scheduling in your applications.

## Installation

Install schedule.do using pnpm:

```bash
pnpm add schedule.do
```

## Basic Setup

### 1. Import

```typescript
import { schedule } from 'schedule.do'
```

### 2. Configure

```typescript
// Optional configuration
const options = {
  // Configuration options
}
```

### 3. Use

```typescript
const result = await schedule({
  // Your data
})

console.log('Result:', result)
```

## First Example

Here's a complete working example:

```typescript
import { schedule } from 'schedule.do'

async function example() {
  try {
    const result = await schedule({
      data: 'example input',
      options: {
        format: 'json',
      },
    })

    console.log('Success:', result)
    return result
  } catch (error) {
    console.error('Error:', error)
    throw error
  }
}

// Run example
example()
```

## Basic Operations

### Operation 1

```typescript
const result = await schedule.operation1(data)
```

### Operation 2

```typescript
const result = await schedule.operation2(data, options)
```

### Operation 3

```typescript
const results = await schedule.batch([data1, data2, data3])
```

## Configuration Options

schedule.do supports various configuration options:

```typescript
const options = {
  // Option 1
  option1: 'value',

  // Option 2
  option2: true,

  // Option 3
  option3: {
    nested: 'value',
  },
}

const result = await schedule(data, options)
```

## Error Handling

Handle errors appropriately:

```typescript
import { schedule, ScheduleError } from 'schedule.do'

try {
  const result = await schedule(data)
} catch (error) {
  if (error instanceof ScheduleError) {
    console.error('Domain-specific error:', error.message)
  } else {
    console.error('Unexpected error:', error)
  }
}
```

## Next Steps

- [Architecture](./architecture) - Learn about system architecture
- [Best Practices](./best-practices) - Recommended patterns
- [API Reference](../api/reference) - Complete API documentation
- [Examples](../examples/basic-usage) - More code examples

## Common Patterns

### Pattern 1: Simple Usage

```typescript
const result = await schedule(input)
```

### Pattern 2: With Options

```typescript
const result = await schedule(input, {
  option: 'value',
})
```

### Pattern 3: Batch Processing

```typescript
const results = await Promise.all(items.map((item) => schedule(item)))
```

## Integration with SDK

Use with sdk.do:

```typescript
import { schedule } from 'schedule.do'
import { db } from 'sdk.do'

// Fetch data from database
const data = await db.list($.Data)

// Process with schedule.do
const results = await Promise.all(data.map((item) => schedule(item)))
```

## Troubleshooting

### Issue: Installation fails

Verify you're using the correct package name:

```bash
pnpm add schedule.do
```

### Issue: Import error

Ensure the import path is correct:

```typescript
import { schedule } from 'schedule.do'
```

## Learn More

- [Architecture](./architecture) - System design
- [Best Practices](./best-practices) - Recommended patterns
- [Troubleshooting](./troubleshooting) - Common issues

---

Part of the [schedule.do](https://schedule.do) documentation.
