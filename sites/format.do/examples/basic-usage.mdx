---
$id: https://format.do/examples/basic-usage
$type: HowTo
title: Basic Data Formatting Usage
description: Simple examples of using format.do
keywords: [format, example, tutorial, basic, usage]
---

# Basic Data Formatting Usage

Simple, practical examples of using format.do.

## Example 1: Simple Operation

```typescript
import { format } from 'format.do'

// Basic usage
const result = await format({
  data: 'example input',
})

console.log('Result:', result)
```

## Example 2: With Options

```typescript
import { format } from 'format.do'

// With configuration options
const result = await format({
  data: 'example input',
  options: {
    format: 'json',
    validate: true,
  },
})

console.log('Configured result:', result)
```

## Example 3: Error Handling

```typescript
import { format, FormatError } from 'format.do'

async function safeOperation(data: any) {
  try {
    const result = await format({
      data,
      options: { strict: true },
    })
    return { success: true, result }
  } catch (error) {
    if (error instanceof FormatError) {
      console.error('Data Formatting error:', error.message)
      return { success: false, error: error.message }
    }
    throw error
  }
}

const result = await safeOperation('input data')
```

## Example 4: Batch Processing

```typescript
import { format } from 'format.do'

// Process multiple items
const items = ['item1', 'item2', 'item3']

const results = await Promise.all(items.map((item) => format({ data: item })))

console.log('Batch results:', results)
```

## Example 5: With TypeScript

```typescript
import { format } from 'format.do'
import type { FormatOptions, FormatResult } from 'format.do'

// Type-safe operation
const options: FormatOptions = {
  data: 'typed input',
  validate: true,
  format: 'json',
}

const result: FormatResult = await format(options)
console.log('Typed result:', result)
```

## Complete Example

```typescript
import { format } from 'format.do'

async function main() {
  // Initialize
  console.log('Starting format operation...')

  // Process data
  const result = await format({
    data: 'example data',
    options: {
      format: 'json',
      validate: true,
      prettify: true,
    },
  })

  // Handle result
  if (result.success) {
    console.log('Success:', result.data)
  } else {
    console.error('Failed:', result.error)
  }

  return result
}

// Run
main().catch(console.error)
```

## Next Steps

- [Advanced Patterns](./advanced-patterns) - Complex workflows
- [Integration](./integration) - Service integration
- [Real-World Use Case](./real-world-use-case) - Production example

---

Part of the [format.do](https://format.do) documentation.
